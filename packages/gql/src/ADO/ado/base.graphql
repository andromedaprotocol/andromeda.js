query BASEADO($contractAddress: String!, $version: String) {
  ADO {
    ado(address: $contractAddress, version: $version) {
      address
      type
      andr {
        ...andrFragment
      }
    }
  }
}
